pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: '*.{ts,js}'
      run: bunx eslint {staged_files}

    - name: format
      glob: '*.{ts,js,json,md,yml,yaml,sql}'
      run: bunx prettier --check {staged_files}
