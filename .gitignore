# dependencies
node_modules

# build output
dist
out
*.tgz
*.tsbuildinfo

# generated source (rebuilt by `bun run embed-migrations`)
src/store/migrations-data.ts

# coverage
coverage
*.lcov

# Kauri — the .kauri/ directory IS committed so project decisions travel
# with the repo. Only the SQLite WAL/SHM runtime files are excluded.
*.db-wal
*.db-shm

# logs
logs
*.log

# environment files
.env
.env.local
.env.*.local

# editors / IDE
.idea
.vscode
*.swp

# caches
.eslintcache
.cache

# bun build artifacts
*.bun-build

# macOS
.DS_Store
