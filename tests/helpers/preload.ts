/**
 * Bun test preload — runs once before any test file.
 *
 * Phase A Step 1 placeholder. In Phase E this will ensure `dist/kauri`
 * exists for layer-3 (CLI) and layer-4 (MCP) test suites by running
 * `bun build` if the binary is missing.
 */

// intentionally empty for the scaffold step
export {};
